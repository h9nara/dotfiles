# Ghostty Configuration
# https://ghostty.org/docs/config

theme = 3024 day

# 将 option + shift + Enter 绑定为切换 Quick Terminal
# global: 前缀意味着即使 Ghostty 没有焦点，你也可以呼出它
keybind = global:option+shift+enter=toggle_quick_terminal
